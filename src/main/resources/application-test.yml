spring:
  datasource:
    url: jdbc:h2:mem:db_test;MODE=MYSQL
  jpa:
    hibernate:
      ddl-auto: create
custom:
  temp:
    dirPath: c:/temp/medium_test/temp
  genFile:
    dirPath: c:/temp/medium_test